[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    timeout: mark a test with a maximum wall-clock time (requires pytest-timeout to enforce)
