services:
  web:
    build:
      context: ./server
      dockerfile: Dockerfile
    command: python run_dev.py
    volumes:
      - ./server:/usr/src/app
    ports:
      - "5001:5000"
    environment:
      - APP_CONFIG=development
      - FLASK_ENV=development
      - DATABASE_URL=postgresql://coproof:coproofpass@db:5432/coproof_db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_LEAN_QUEUE=lean_queue
      - CELERY_COMPUTATION_QUEUE=computation_queue
      - CELERY_GIT_ENGINE_QUEUE=git_engine_queue
      - CELERY_NL2FL_QUEUE=nl2fl_queue
      - CELERY_AGENTS_QUEUE=agents_queue
      - JWT_SECRET_KEY=dev_jwt_secret_key_do_not_use_in_prod
      - SECRET_KEY=dev_secret_key_do_not_use_in_prod
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      lean-worker:
        condition: service_started
      computation-worker:
        condition: service_started
      nl2fl-worker:
        condition: service_started
      agents-worker:
        condition: service_started

  celery_worker:
    build:
      context: ./server
      dockerfile: Dockerfile
    command: celery -A celery_worker.celery worker -Q git_engine_queue --loglevel=info
    volumes:
      - ./server:/usr/src/app
    environment:
      - APP_CONFIG=development
      - DATABASE_URL=postgresql://coproof:coproofpass@db:5432/coproof_db
      - REDIS_URL=redis://redis:6379/0
      - CELERY_LEAN_QUEUE=lean_queue
      - CELERY_COMPUTATION_QUEUE=computation_queue
      - CELERY_GIT_ENGINE_QUEUE=git_engine_queue
      - CELERY_NL2FL_QUEUE=nl2fl_queue
      - CELERY_AGENTS_QUEUE=agents_queue
      - REPO_STORAGE_PATH=/tmp/coproof-storage
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
      lean-worker:
        condition: service_started
      computation-worker:
        condition: service_started
      nl2fl-worker:
        condition: service_started
      agents-worker:
        condition: service_started

  lean-worker:
    build:
      context: ./lean
      dockerfile: Dockerfile
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CELERY_LEAN_QUEUE=lean_queue
    depends_on:
      redis:
        condition: service_started

  computation-worker:
    build:
      context: ./computation
      dockerfile: Dockerfile
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CELERY_COMPUTATION_QUEUE=computation_queue
    depends_on:
      redis:
        condition: service_started

  nl2fl-worker:
    build:
      context: ./nl2fl
      dockerfile: Dockerfile
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CELERY_NL2FL_QUEUE=nl2fl_queue
      - CELERY_LEAN_QUEUE=lean_queue
      - COPILOT_BASE_URL=http://host.docker.internal:8000
    depends_on:
      redis:
        condition: service_started
      lean-worker:
        condition: service_started

  agents-worker:
    build:
      context: ./agents
      dockerfile: Dockerfile
    environment:
      - REDIS_URL=redis://redis:6379/0
      - CELERY_AGENTS_QUEUE=agents_queue
      - COPILOT_BASE_URL=http://host.docker.internal:8000
    depends_on:
      redis:
        condition: service_started

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    depends_on:
      - web

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=coproof
      - POSTGRES_PASSWORD=coproofpass
      - POSTGRES_DB=coproof_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U coproof -d coproof_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
